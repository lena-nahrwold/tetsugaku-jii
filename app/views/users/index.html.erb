<div class="container">
  <div class="row">
    <h3>Users</h3>
      <table class="table">
        <tbody>
          <% @users.order('created_at DESC').each do |user| %>
            <tr>
              <%= render user %>
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>
</div>
